<div class="flex flex-row">
    <img class="object-cover border-blue-900 border-2" src="{{asset("build/images/person.png")}}" alt="image profil" />
      <p class="bg-blue-900 text-white pl-3 pr-3 rounded-r-lg place-self-center">
        {{ app.user.name }}
      </p>
    <a class="self-center ml-5 bg-red-500 pl-3 pr-3 rounded-2xl" href="{{ path('app_logout') }}" >déconnexion</a>
  </div>
